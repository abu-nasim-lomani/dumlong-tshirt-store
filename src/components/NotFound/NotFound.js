import React from 'react';
import Header from '../Header/Header';

const NotFound = () => {
    return (
        <div>
            <Header></Header>
            <h1 style={{textAlign: 'center'}}>File Not Found</h1>
        </div>
    );
};

export default NotFound;